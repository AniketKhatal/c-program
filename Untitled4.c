//Program Name:
//Write a c program to with following function to compare given two
//string. (don’t use inbuilt function)
// int cmpString(char[],char[])

#include<stdio.h>

int main()     //main function
{
	char ch1[10],ch2[10];
	int i;
	printf("Enter the str 1:");
	scanf("%c",&ch1[i]);
	gets(ch1);
	//printf("The atring is :%s",ch1);
	printf("\nEnter the str 2:");
	gets(ch2);
	printf("The atring is :%s",ch2);
//	if(ch[i])
	return 0;
}

