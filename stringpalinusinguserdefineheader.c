//Program Name: to check the string is palindrome or not

#include<stdio.h>
#include<anikstr.h>
int main()     //main function
{
	char ch[20];
	printf("Enter the string:");
	gets(ch);
	palinstr(ch);
	return 0;
}

