//caesar cipher Algorithm 

#include<stdio.h>
int main()
{
	int i,j,a[10],key;
	printf("enter the plain text into array: ");
	for(i=0;i<6;i++)
	{
		scanf("%d",&a[i]);
	}
	printf("enter the key :");
	scanf("%d",&key);
	printf("the array :");
	for(i=0;i<6;i++)
	{
		printf("%d",a[i]);
	}
	for(i=0i<6;i++)
	{
		if(a[i]=' ')
		{
			continue;
		}
		else if(a[i])
		
	}
	return 0;
}
